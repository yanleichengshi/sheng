spring:
  application:
    name: orders
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        group: orders-group
        server-addr: localhost:8848
        namespace: my_naocs
      config:
        server-addr: localhost:8848
        group: orders-group
        namespace: my_naocs
        file-extension: yaml
        refresh-enabled: true